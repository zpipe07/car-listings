import React from 'react';

import './Header.css';

const Header = (props) => {
  return (
    <header className="Header">
      <h2 className="Header__title">
        Sort, search, and browse hundreds of cars.
      </h2>
    </header>
  );
};

export default Header;
